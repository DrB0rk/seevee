---
/**
 * Render diagnostics placeholder. The real panel fetches
 * `/api/render/inspect?presentationId=…` once a presentation is active.
 */
---

<div class="diagnostics-placeholder">
  <p>No diagnostics yet — pick a presentation to inspect.</p>
</div>